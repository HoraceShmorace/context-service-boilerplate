var mongoose = require('mongoose'),
    Controller = require('./controller');

var Contextualizer = function(config) {
    mongoose.connect(config.dbUrl);
    return Controller;
};

module.exports = Contextualizer;
